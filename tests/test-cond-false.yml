name: Test condition false
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          cond: 'false'
          if_true: 'hello'
          if_false: 'world'
      - name: Assert output equals if_false value
        env:
          ACTUAL: ${{ steps.action.outputs.value }}
        run: |
          echo "actual: $ACTUAL"
          if [ "$ACTUAL" != "world" ]; then
            echo "FAIL: expected 'world' but got '$ACTUAL'"
            exit 1
          fi
          echo "PASS"
